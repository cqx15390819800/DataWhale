Task2 EDA

该数据集的缺失并不多：
bodyType，fuelType，gearbox，还有一个隐藏的notRepairedDamage特征
由于这些特征缺失并不过多，所以可以考虑进行填补，可以用均值，中位数，众数等填补，
还可以使用随机森林等模型进行填补
对于三板斧模型，即使缺失，模型也能学习

数据异常上：
power特征存在长尾分布，而且很多值存在异常，例如power为0且大于600等，
因此在对数据进行处理时可以考虑删除（但不能删除测试集），也可以进行对数化，分箱等方式构建特征
由于power为0导致模型不好预测，所以宁愿将power=0的数据改为NA，这样反而可以增加模型的鲁棒性

经过分析，也发现seller和offerType特征存在单一性，完全可以删除

数据的分布：
price明显不符合正态分布，这样不符合模型的假设，不利于模型的学习，因此将price进行对数化处理，处理后效果很好。
并且通过skew和kurt进行数据的统计分析

对于数值特征，对三种分布进行分析：
自己的分布distplot，热力图可视化两两之间的相关性heatmap，以及特征之间的pairplot，regplot等。。。
通过观察发现有些匿名特征存在很大的相关性，所以这一点我们可以用到模型融合上。

对于类别特征：
观察其nunique分布，箱型图，小提琴图，柱状图，以及频数可视化

最后介绍了用pandas_profiling生成数据报告

总结：
EDA的主要作用是加强对数据的了解，知道什么样的特征应该做相应的处理，
知道什么样的特征利于学习，什么样的特征对模型的学习毫无作用，反而会使模型变得复杂，
通过对数据以及业务的了解，才能构建出更好的特征让模型学习，效果才会更佳。











